# Dependencies
node_modules

# Build output
dist
dist-ssr
*.local

# Rust / Tauri build artefacts
src-tauri/target
src-tauri/gen

# Local scratch work, not part of the product
server

# Editors and OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea

# Environment and secrets — never commit these
.env
.env.*
!.env.example
TODO.md

# Updater signing keys. The private key lives in GitHub Secrets and in an
# offline backup; if it ever reaches the repository, anyone who can read the
# repository can sign an update that installs itself on every user's machine.
*.key
!*.key.pub
minisign*.key
