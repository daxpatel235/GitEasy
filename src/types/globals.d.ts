/**
 * Build-time constants injected by Vite.
 *
 * `__APP_VERSION__` is read from package.json at build time, so the version the
 * About screen shows is always the one that was actually shipped.
 */
declare const __APP_VERSION__: string;
